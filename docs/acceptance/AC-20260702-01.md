---
acceptance_id: AC-20260702-01
task_id: TASK.semantic-stale-ranking-export
intent_id: INT-001
owner: memx-resolver
status: approved
reviewed_at: 2026-07-02
reviewed_by: codex
---

# Acceptance Record: Semantic Stale And Prompt Memory Export

## Scope

- 対象変更:
  - `docs/memx_spec_v3/go/service/`
  - `docs/memx_spec_v3/go/api/`
  - `docs/memx_spec_v3/go/cmd/mem/`
  - `docs/memx_spec_v3/go/db/migrate_resolver.go`
  - `docs/memx_spec_v3/docs/contracts/`
  - `tests/test_cli_schema_contract.py`
  - `docs/requirements.md`
  - `docs/interfaces.md`
  - `docs/design.md`
  - `README.md`
  - `USER_GUIDE.md`
- 非対象:
  - `agent-taskstate` DB への direct write
  - 外部 tracker への同期

## Acceptance Criteria

- [x] stale 判定が version mismatch だけでなく read receipt の chunk snapshot と最新版 chunk の semantic diff を返せる
- [x] stale reason に `severity`、`impact_scope`、`changed_chunks` が含まれる
- [x] memory card ranking が configurable weights と feedback log で補正できる
- [x] `mem docs cards-feedback` / `/v1/cards:feedback` で利用ログを記録できる
- [x] `mem docs bundle` / `/v1/cards:bundle` で prompt-ready bundle を取得できる
- [x] `mem docs taskstate-export` / `/v1/taskstate:export` で agent-taskstate 連携 payload を取得できる
- [x] CLI JSON / HTTP OpenAPI の実レスポンスが schema 契約に適合する

## Evidence

- 実行コマンド:
  - `python -m pytest tests`
  - `$env:GOCACHE='.tmp\go-build'; go test ./...`
  - `python -m json.tool docs\memx_spec_v3\docs\contracts\cli-json.schema.json`
  - `python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('docs/memx_spec_v3/docs/contracts/openapi.yaml').read_text(encoding='utf-8'))"`
- テスト結果:
  - Python tests: passed
  - Go tests: passed
  - CLI JSON schema syntax: passed
  - OpenAPI YAML parse: passed

## Verification Result

- 判定: approved
- コメント:
  - semantic stale、adaptive ranking、prompt bundle、agent-taskstate export bridge が API / CLI / schema / tests に揃った。
