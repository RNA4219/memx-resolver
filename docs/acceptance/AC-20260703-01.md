---
acceptance_id: AC-20260703-01
task_id: TASK.resolver-extension-sequence
intent_id: INT-001
owner: memx-resolver
status: approved
reviewed_at: 2026-07-03
reviewed_by: codex
---

# Acceptance Record: Resolver Search, Audit, Export, Migration, Version Policy

## Scope

- 対象変更:
  - `docs/memx_spec_v3/go/db/migrate_resolver.go`
  - `docs/memx_spec_v3/go/service/`
  - `docs/memx_spec_v3/go/cmd/mem/`
  - `docs/memx_spec_v3/docs/contracts/`
  - `README.md`
  - `USER_GUIDE.md`
  - `docs/requirements.md`
  - `docs/interfaces.md`
  - `docs/design.md`
  - `docs/EVALUATION.md`
- 非対象:
  - `agent-taskstate` DB への direct write
  - 外部 tracker への本格同期

## Acceptance Criteria

- [x] resolver 専用 FTS5 が `docs:search` / `cards:search` で利用される
- [x] read receipt が `previous_receipt_hash` / `receipt_hash` を保存し、`resolver_audit_log` に `reads_ack` を記録する
- [x] `tracker_refs` / `birdseye_refs` が `taskstate-export` の `source_refs` に含まれる
- [x] `mem docs migrate-resolver-store --from short.db --to resolver.db --dry-run` と実移行が動作する
- [x] `version_scheme` が `semver` / `iso_datetime` / `git_revision` / `string` を扱い、SemVer 表記差による stale 誤検知を避ける
- [x] CLI JSON / HTTP OpenAPI 契約が追加フィールドと互換である

## Evidence

- 実行コマンド:
  - `go test ./service ./db ./cmd/mem`
  - `go test ./...`
  - `python -m pytest tests`
  - `python -m json.tool docs\memx_spec_v3\docs\contracts\cli-json.schema.json`
  - `python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('docs/memx_spec_v3/docs/contracts/openapi.yaml').read_text(encoding='utf-8'))"`
- テスト結果:
  - Go targeted tests: passed
  - Go all packages: passed
  - Python tests: passed
  - CLI JSON schema syntax: passed
  - OpenAPI YAML parse: passed

## Verification Result

- 判定: approved
- コメント:
  - 5つの拡張候補を順に実装し、resolver store / search / audit / export / version policy の主要フローを自動テストで確認した。
